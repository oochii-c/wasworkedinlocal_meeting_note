# 회의록 자동화 시스템 — 1주차 기획 문서

---

## 1. 유저 시나리오 (누가, 언제, 어떤 순서로 쓰는가)

### 기본 시나리오
1. **회의 시작 전** — 사용자가 웹/앱에서 "새 회의 시작" 버튼을 누른다.
2. **회의 중** — 시스템이 마이크로 음성을 실시간으로 녹음(또는 파일 업로드)한다.
3. **회의 종료 후** — 사용자가 "종료 및 정리" 버튼을 누른다.
4. **자동 처리** — 시스템이 음성을 텍스트로 변환(STT)하고, 발언자를 구분하고, 핵심 내용을 요약한다.
5. **결과 확인** — 사용자가 회의록(전체 텍스트 + 요약 + 액션 아이템)을 화면에서 확인한다.
6. **공유/저장** — 사용자가 회의록을 PDF/링크 형태로 팀원에게 공유하거나 저장한다.

### 예시 (구체적 인물 시나리오)
> 팀장 A는 매주 월요일 오전 10시 주간 회의를 진행한다. 회의 시작 시 노트북에서 녹음 버튼을 누르고, 40분간 회의를 진행한다. 회의가 끝나면 종료 버튼을 누르고, 5분 뒤 자동으로 정리된 회의록(참석자별 발언 요약 + 결정 사항 + 할 일 목록)을 슬랙 채널에 자동으로 공유받는다.

### 주요 사용자 유형
| 사용자 | 니즈 |
|---|---|
| 회의 주최자 | 빠르게 녹음 시작/종료, 회의록 자동 공유 |
| 회의 참석자 | 나중에 놓친 내용 검색·확인 |
| 관리자 | 여러 회의록을 한 곳에서 관리 |

---

## 2. 개발 기획서 (만들 기능 / 만들지 않을 기능)

### 이번 주에 만들 기능 (MVP)
- [ ] 음성 파일 업로드 또는 실시간 녹음 기능
- [ ] STT(Speech-to-Text) 변환 — 음성 → 텍스트
- [ ] 텍스트 자동 요약 (핵심 논의사항 정리)
- [ ] 액션 아이템(할 일) 자동 추출
- [ ] 회의록 화면 출력 및 텍스트 다운로드(PDF/텍스트 파일)
- [ ] 회의 기록 목록 조회 (지난 회의록 리스트)

### 이번 주에 만들지 않을 기능 (Out of Scope)
- 다국어 지원 (한국어만 우선 지원)
- 화자 인식 고도화 (누가 말했는지 정밀 구분 — 1차는 생략 가능)
- 실시간 자막 표시
- 슬랙/이메일 자동 발송 연동
- 모바일 앱 버전
- 회의 영상 녹화

### 우선순위
1순위: 음성→텍스트 변환이 정확히 되는가
2순위: 요약이 핵심을 잘 담아내는가
3순위: 결과물을 보기 좋게 출력하는가

---

## 3. 시스템 설계도 (화면 · 데이터 · 결과물의 연결)

### 전체 흐름
```
[사용자] 
   ↓ 녹음 시작 / 파일 업로드
[프론트엔드 화면]
   ↓ 오디오 파일 전송
[백엔드 서버]
   ↓ 오디오 → STT API 호출
[텍스트 변환 결과] (원본 스크립트)
   ↓ 요약 모델(LLM) 호출
[요약 + 액션 아이템 추출 결과]
   ↓ 데이터베이스 저장
[DB: 회의록 테이블]
   ↓ 조회
[프론트엔드 화면] → 사용자에게 최종 회의록 표시
```

### 화면 구성 (3개 페이지)
1. **홈/녹음 화면** — 녹음 시작·종료, 파일 업로드
2. **처리중 화면** — 변환/요약 진행 상태 표시
3. **회의록 상세 화면** — 원본 텍스트 + 요약 + 액션 아이템 + 다운로드 버튼

### 데이터 구조 (예시)
| 테이블 | 주요 필드 |
|---|---|
| meetings | id, 제목, 날짜, 참석자, 상태(진행중/완료) |
| transcripts | meeting_id, 원본 텍스트, 생성시각 |
| summaries | meeting_id, 요약 내용, 액션 아이템 리스트 |

### 사용 예상 기술 스택
- STT: Whisper API 또는 유사 음성인식 API
- 요약: Claude/GPT 등 LLM API
- 백엔드: Node.js 또는 Python (Flask/FastAPI)
- DB: SQLite/PostgreSQL
- 프론트엔드: React

---

## 4. 그 외 필요 문서

### 4-1. 위험 요소 및 대응 방안
| 위험 | 대응 |
|---|---|
| 음성 인식 정확도 낮음 (잡음, 사투리 등) | 조용한 환경 가정, 추후 개선 |
| 요약이 핵심을 놓침 | 프롬프트 튜닝, 사용자 재요청 기능 |
| 긴 회의(1시간+) 처리 시간 오래 걸림 | 비동기 처리 + 진행률 표시 |

### 4-2. 성공 기준 (이번 주 완료 조건)
- 5분 내외 테스트 음성을 업로드하면 텍스트 변환 + 요약까지 자동으로 완료된다.
- 결과 화면에서 원본/요약/할 일이 구분되어 보인다.
- 결과를 텍스트 파일로 다운로드할 수 있다.

### 4-3. 향후 확장 아이디어 (이번 주 범위 밖, 메모용)
- 화자별 발언 분리
- 팀 협업 도구(슬랙, 노션) 자동 연동
- 회의 트렌드 분석 대시보드
